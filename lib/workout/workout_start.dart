import 'package:flutter/material.dart';

class WorkoutStart extends StatelessWidget {
  const WorkoutStart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(),
    );
  }
}
